# DECISION LOG

> Toda decisión core con hora y motivo.
> Cuando a las 20:00 alguien pregunte "¿por qué no hicimos X?", está escrito acá.


| Hora | Decisión | Motivo |
|---|---|---|
| 28/08 pre-evento | Los 4 briefs se desarrollan en `docs/05-challenges.md` (resumen en español + análisis vs. criterios del kickoff + 2-3 ángulos por challenge). Challenge **aún sin elegir**. — Samo | Los briefs ya son públicos; pre-leerlos deja llegar al kickoff con una candidata y una Y en una frase, sin quemar los primeros 20 min leyendo en frío. Se actualizaron `00-dominios.md` y `01-kickoff.md` para asumir briefs conocidos. |
| 29/08 pre-evento | **Challenge elegido: C2 · The Control Tower (Yuno).** — Samo / equipo | Menor riesgo de crash en vivo que C4, más defendible técnicamente que C3, y el más alineado con el patrón "datos fragmentados → IA aísla y explica → acción". |
| 29/08 pre-evento | Contexto de dominio (pagos) en `docs/06-dominio-pagos.md`. Decisiones técnicas core **sin tomar**: se resuelven en la reunión de arranque con `docs/07-decisiones-core.md` como disparador. — Samo | El research (`06`) da recomendaciones, pero el equipo todavía no ratificó ninguna. Este log se completa a medida que se decidan. |
| 29/08 post-mentores | **Alcance de la demo: Opción A + los 2 incidentes simultáneos** (no se recorta a bonus). — mentores + equipo | Los mentores lo bancan; separar dos causas a la vez es lo que muestra que el localizador es de verdad. |
| 29/08 post-mentores | **Banco emisor NO es eje de búsqueda del cubo** — entra como evidencia del slice ganador, igual que el código de rechazo. — mentores | Simplifica el cubo sin perder la narrativa. Recomendación de dimensiones: `merchant × proveedor × método × país` (a confirmar). |
| 29/08 post-mentores | **Diferenciador = ventana de tiempo near real time.** **Valor de producto = la acción recomendada de calidad.** — mentores | Guían dónde poner el esfuerzo: latencia baja visible + catálogo de acciones bien hecho, no más features. |
| 29/08 post-mentores | **Arquitectura: pipeline de 5 capas, `docs/arquitectura.html`.** Generador → Motor determinístico (Detector → Localizador) → Agente RAG → Playbook + templates → Entrega. Regla: **lo determinístico calcula, la IA entiende**. El motor NO usa LLM. — equipo (pizarrón) | La detección y la localización tienen que ser correctas en vivo (trial by fire) → no pueden depender de un modelo. El LLM interpreta y redacta, no busca la causa. |
| 29/08 post-mentores | **Capa IA en 2 etapas distintas.** (1) **Agente RAG**: interpreta el output estructurado del motor contra una base de conocimiento en archivos `.md` (reglas de negocio de pagos), saca conclusión + severidad + categoría de acción. (2) **Playbook**: catálogo propio de acciones de remediación + templates; el LLM elige la que aplica y la redacta. — equipo | Meter todas las reglas de negocio en el prompt es complejidad y ruido; la base `.md` las mantiene ordenadas y editables. El catálogo propio da control sobre la calidad de la acción recomendada. |
| 29/08 post-mentores | **La base de conocimiento `.md` es estática** — la escribimos nosotros antes del evento, no se toca en la demo. Lo que varía en el trial by fire es el incidente que inyecta el juez, no las reglas. — equipo | Editar reglas en vivo es frágil y no suma; el valor a demostrar es que el sistema diagnostica un incidente no ensayado. |
| 29/08 post-mentores | **Entrega dual:** dashboard (la torre de control + panel de inyección del juez) **y** un canal ejecutivo tipo Slack/WhatsApp para el resumen en una línea. — equipo | El dashboard es lo que se opera en el trial by fire; el canal es el guiño al Payments Concierge de Yuno y cubre el bonus de "dos audiencias". |
| 29/08 post-mentores | **Acción recomendada en la demo: se muestra + botón "aplicar (simulado)"**, marcado claramente como simulación. El challenge **diagnostica, no ejecuta** — el loop de vuelta al generador existe solo para la demo. — equipo | Mostrar el efecto de la acción sobre el stream es vistoso; el marcado explícito evita que el jurado diga "esto era diagnosticar, no remediar". |
| 29/08 post-mentores | **Hito: mini demo end-to-end a las ~2 h** (todo falso: generador emitiendo, detector disparando sobre una caída hardcodeada, explicación stub, dashboard mostrando el incidente). — recomendación de mentor | Fuerza a tener el esqueleto conectado temprano y a atacar el spike de riesgo primero. Alcance exacto a confirmar con el mentor. |
| 29/08 producto/UX | **Dos superficies, una sola experiencia:** landing pública en `/` y panel operacional en `/control-tower`, compartiendo marca, lenguaje y componentes. La landing vende la promesa; el panel demuestra y opera el diagnóstico. — equipo / Juani | Separar persuasión de operación evita convertir la landing en un dashboard ilegible o el panel en una pieza de marketing. El CTA principal conecta ambas superficies y abre la demo real. |
| 29/08 producto/UX | **Autoridad visual y de marketing: Payments Concierge de Yuno.** Base negra/blanca, tipografía Geist, grilla técnica, titulares grandes y acento azul-violeta reservado. No se copia la página; se construye una extensión reconocible de la familia Yuno. — Juani | Hace que el prototipo parezca una capacidad integrable a Yuno y no una marca paralela. Nuestro diferencial visual y narrativo será la evidencia operacional, no decoración de IA. |
| 29/08 producto/UX | **La demo comienza con un stream sintético controlado desde la interfaz:** estados `READY / RUNNING / PAUSED / COMPLETE`, acciones `Start live stream / Pause / Reset` y badge persistente `SIMULATION MODE`. — equipo / Juani | Permite iniciar la secuencia sin teclado, repetir fixtures de aprobados y rechazados y ser transparentes con el jurado sobre el origen de los datos. |
| 29/08 producto/UX | **Composición del panel:** Command Center como vista principal y Incident Detail como profundización. La comparación temporal, los incidentes priorizados y el dinero en riesgo son el primer nivel; evidencia, ownership, confianza y acción recomendada son el segundo. — equipo / Juani | Conserva la complejidad útil de una herramienta operacional en tiempo real, pero sostiene una jerarquía clara para que el jurado entienda el diagnóstico en segundos. |
| 29/08 producto/UX | **Nombre del producto: Centinel**, con esa ortografía exacta. Descriptor: `Payment operations control tower`. — Juani / equipo | Es breve, pronunciable y expresa vigilancia permanente sin convertir el nombre en una explicación funcional larga. |
| 29/08 producto/UX | **Composición aprobada: A para Command Center + C como vista separada de investigación.** A es el inicio operacional; `Why this diagnosis?` navega a C. — Juani | A comunica estado y tiempo real de inmediato. C ofrece la profundidad necesaria para contrastar merchant/provider logs sin sobrecargar la pantalla principal. |
| 29/08 producto/UX | **Toda superficie de producto, landing, demo y pitch se presenta en inglés.** Los documentos internos del harness permanecen en español. — Juani / equipo | Cumple el formato del evento y evita mezclar idiomas frente al jurado, sin empeorar la velocidad de coordinación interna. |
| 29/08 producto/UX | **Contrato de datos UI:** Centinel consume `transactions`, `baseline_profile` e `incidents` mediante un adapter tipado; los cuatro ejes localizan y banco/código/latencia actúan como evidencia. Los IDs `BIGSERIAL` se serializan como strings. — equipo | Evita acoplar la interfaz al SQL, conserva precisión de IDs y separa dimensiones de diagnóstico de la evidencia que lo sustenta. |
| 29/08 integración | **Ventana operacional por defecto: últimos 60 s contra baseline contextual de 14 días.** La vista histórica de 2 h queda como exploración, no como hot path de detección. La demo usa entidades de PagoTotal y códigos ISO 8583 del plan de datos. — Juani / Pena | Alinea el frontend con el ring buffer, el forecast y los endpoints reales; elimina códigos y nombres mock que el backend nunca produciría. |
| 29/08 frontend | **La secuencia visual preserva silencio saludable antes de revelar incidentes; Pause congela el estado y Reset vuelve a healthy.** Los selectores de ventana/scope quedan explícitamente fijos hasta conectarlos al backend. `/demo-control` permanece fuera de `frontend/` porque el plan de ejecución lo asigna a Samo. — Juani | Evita que READY contradiga al gráfico, hace la demo reproducible y respeta las fronteras de trabajo del equipo sin presentar controles decorativos como terminados. |
| 29/08 producto/IA | **Firma de marca: `Centinel by toons`.** La configuración en lenguaje natural sólo crea borradores estructurados: validación, replay y aprobación humana son obligatorios. Diagnóstico, explicación, recomendación, decisión y resultado se guardan separados. — Juani | Distingue autoría de la referencia Yuno y evita que el LLM altere silenciosamente criterios críticos o borre el rastro entre evidencia y acción. |
| 29/08 frontend | **El product preview de la landing participa del flujo normal:** `headline → explicación → CTAs → dashboard`; sólo invade de forma controlada la sección siguiente. — Juani | Posicionarlo desde el borde inferior con altura implícita hacía que el dashboard subiera y tapara CTAs cuando crecían sus métricas. El flujo explícito elimina ese acoplamiento y mantiene la jerarquía en distintas alturas de pantalla. |
| 29/08 producto/UX | **Centinel deja de presentarse como dashboard y pasa a una investigación guiada:** `Observe → Detect → Validate → Diagnose → Compare/Explain → Decide → Monitor`. `Centinel Copilot` vive como rail contextual del incidente y una comparación conversacional forma parte del demo path P0. — Juani | La ventaja no es mostrar más métricas, sino conducir desde la anomalía hasta una decisión defendible. El Copilot interpreta agregados y evidence IDs producidos por el pipeline; no calcula sobre transacciones crudas ni modifica reglas. Si OpenAI falla, la misma evidencia se presenta con una respuesta estructurada determinística. |
| 29/08 frontend/UX | **La investigación no navega a una pantalla `/incidents/:id`: abre un workspace inline dentro de `/control-tower`.** El Command Center permanece como contexto visual detrás y la URL no cambia. — Juani | Evita romper la continuidad justo cuando aparece una alerta y permite contrastar métricas, cola, evidencia y conversación como partes de una sola sesión operacional. La ruta vieja se normaliza a `/control-tower`. |
| 29/08 frontend/UX | **Los CTA de la landing usan una transición de entrada de 1,45 s antes de abrir `/control-tower`.** Un objeto orbital representa los cuatro ejes del cubo y comunica `Synchronizing merchant and provider truth`; respeta reduced motion. — Juani | Convierte el cambio de superficie en parte del relato de producto y evita un salto seco entre marketing y operación, sin agregar una dependencia visual externa ni una espera larga a la demo. |
| 29/08 producto/UX | **La cabecera operacional separa `Live Monitoring` de `Explore`.** Explore admite dos rangos históricos personalizados y scope por dimensiones; una consulta natural puede compilarse a filtros visibles antes de `Run analysis`. Centinel también podrá proponer criterios de alerta, pero sólo como `PolicyDraft` gobernado; queda abierta la interacción `LLM vs inputs`. — Juani | Separa detección automática, análisis ad hoc y configuración de políticas para que el gráfico tenga una pregunta explícita. Mantiene potencia exploratoria sin permitir que una instrucción ambigua altere el detector ni comprometa el trial by fire. |
| 29/08 pitch | **Formato de presentación: 2:00 de producto/introducción + 5:00 de demo en vivo.** La demo reserva 1:15 final para trial by fire; `Explore + PolicyDraft` tiene un máximo de 0:40 y es el primer bloque a cortar si el ensayo supera siete minutos. — Juani | Protege los resultados obligatorios del challenge y evita que una capacidad P1 consuma el tiempo destinado a evidencia, incidentes simultáneos, incertidumbre y prueba del juez. |
| 29/08 frontend/UX | **Los controles de `Live Monitoring` pasan a ser interactivos para la vista:** observed window (`60 s / 5 min / 15 min`), referencia (`14-day / previous period / contract threshold`) y scope predefinido. Cambian KPIs, título y contexto del gráfico; no reconfiguran la política del detector. `Reset` restaura la vista default. — Juani | Permite consultar el stream sin entrar a Explore y elimina controles decorativos, manteniendo separada la navegación visual de la configuración gobernada de alertas. |
| 29/08 post-mentores | **Abiertas** (sin decidir): cardinalidades y throughput del generador · números de latencia (`< 45 s` alerta / `< 90 s` diagnóstico, propuestos) · overlap permitido entre los 2 incidentes. **Cerradas:** nombre Centinel y cubo `merchant × provider × method × country`; banco/código quedan como evidencia. | Las abiertas se cierran en las primeras horas del sábado; las cerradas ya tienen contrato de producto y datos. |
| 29/08 integración | **La capa de explicación es de Samo:** recibe el `EngineOutput` determinístico de Luca, selecciona una acción desde catálogo, obtiene el costo de `money_lost` de Pena y sólo usa Pydantic AI sin tools para redactar en inglés. | Mantiene detección, localización, clasificación y decisión de acción auditables; si OpenAI o datos no están disponibles, templates y fallback monetario conservan la demo. |
| 30/08 explain | **Capa de explicación implementada y verificada** en `backend/explain/` (contratos en `backend/contracts.py`, API `POST /api/agent/explain` en `backend/main.py`). El **`headline` es determinístico** (template con la plata + el slice), no lo escribe el LLM. El LLM (`gpt-4o`, temp 0, timeout 10 s) solo redacta `executive` / `operations` / `action_rationale`. Sin key o sin `pydantic-ai` → fallback Jinja con la misma evidencia y acción. — Samo | El LLM erraba el `headline` (ponía la acción en vez de la situación+plata). Tono verificado contra `08-product-ux-gtm.md` §11 en los 3 fixtures: "the evidence indicates", sin alarmismo, sin cuantificar cuando no hay costo. |
| 30/08 explain | **Prioridad de incidentes = `impacto × alcance × persistencia × confianza × criticidad_merchant`**, con `impacto` relativo al máximo del batch (no cap fijo). Cada componente queda expuesto en la respuesta. — Samo | Con cap fijo a $10k, dos incidentes grandes topaban en 1.0 y la plata dejaba de diferenciar; el de más plata quedaba segundo, invirtiendo la narrativa del pitch. |
| 29/08 spike detector | **Primer corte implementado por país: CUSUM binomial unilateral por slice + baseline horario opcional + estado independiente por país.** Los thresholds quedan provisionales hasta calibrarlos contra el replay final. | El approval rate nace de éxitos/intentos binomiales: usar ambos conteos incorpora el volumen y evita tratar igual una caída con 5 intentos que otra con 500. Un estado por país permite abrir, priorizar y resolver dos incidentes simultáneos sin mezclar identidades. |
| 29/08 contrato diagnóstico | **`diagnosis_category` se decide de forma determinística en `/backend/core`, después del localizador y antes de RAG/playbook.** Se implementa como módulo separado del detector; combina slice, decline shift, evidencia de issuer y confianza. El LLM puede redactar, pero no elegir ni cambiar la categoría. | Mantiene el trial by fire reproducible y establece una frontera clara: el core detecta, localiza y clasifica; el playbook selecciona la acción; el LLM comunica. Un mapeo de código aislado no alcanza porque el mismo código puede implicar causas distintas según la forma del slice y los segmentos de control. |
| 29/08 integración core-datos | **La entrada canónica del core es `get_cube(60s)` con 81 hojas; `process_events` queda como adaptador offline/fallback. Issuer y decline code entran después, vía `get_evidence(filters, 60s)`.** | Evita duplicar el agregador de Pena en el hot path, conserva una forma de probar el motor aisladamente y limita el costo de evidencia a los slices que el localizador realmente seleccionó. |
| 29/08 incidentes superpuestos | **El localizador usa residualización multiplicativa: luego de elegir una causa, descuenta su ripple del déficit y del baseline todavía disponible antes de buscar la siguiente.** | Permite separar dos incidentes simultáneos aunque compartan país y hojas del cubo, sin convertir issuer ni decline code en dimensiones y sin pedirle al LLM que resuelva la ambigüedad. |
| 30/08 contrato core-explain | **La taxonomía determinística del core permanece canónica. La capa explain traduce `issuer_over_declining → issuer_declining` y `payment_method_outage → method_degradation` sólo para seleccionar templates/playbooks, y agrega acciones propias para `merchant_integration_error` y `merchant_configuration`.** | Evita cambiar el veredicto auditable emitido por el motor y permite que todas sus categorías soportadas lleguen a una recomendación determinística en la capa de Samo. |
