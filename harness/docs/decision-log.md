# DECISION LOG

> Toda decisión core con hora y motivo.
> Cuando a las 20:00 alguien pregunte "¿por qué no hicimos X?", está escrito acá.


| Hora | Decisión | Motivo |
|---|---|---|
| 28/08 pre-evento | Los 4 briefs se desarrollan en `docs/05-challenges.md` (resumen en español + análisis vs. criterios del kickoff + 2-3 ángulos por challenge). Challenge **aún sin elegir**. — Samo | Los briefs ya son públicos; pre-leerlos deja llegar al kickoff con una candidata y una Y en una frase, sin quemar los primeros 20 min leyendo en frío. Se actualizaron `00-dominios.md` y `01-kickoff.md` para asumir briefs conocidos. |
| 29/08 pre-evento | **Challenge elegido: C2 · The Control Tower (Yuno).** — Samo / equipo | Menor riesgo de crash en vivo que C4, más defendible técnicamente que C3, y el más alineado con el patrón "datos fragmentados → IA aísla y explica → acción". |
| 29/08 pre-evento | Contexto de dominio (pagos) en `docs/06-dominio-pagos.md`. Decisiones técnicas core **sin tomar**: se resuelven en la reunión de arranque con `docs/07-decisiones-core.md` como disparador. — Samo | El research (`06`) da recomendaciones, pero el equipo todavía no ratificó ninguna. Este log se completa a medida que se decidan. |
| 29/08 post-mentores | **Alcance de la demo: Opción A + los 2 incidentes simultáneos** (no se recorta a bonus). — mentores + equipo | Los mentores lo bancan; separar dos causas a la vez es lo que muestra que el localizador es de verdad. |
| 29/08 post-mentores | **Banco emisor NO es eje de búsqueda del cubo** — entra como evidencia del slice ganador, igual que el código de rechazo. — mentores | Simplifica el cubo sin perder la narrativa. Recomendación de dimensiones: `merchant × proveedor × método × país` (a confirmar). |
| 29/08 post-mentores | **Diferenciador = ventana de tiempo near real time.** **Valor de producto = la acción recomendada de calidad.** — mentores | Guían dónde poner el esfuerzo: latencia baja visible + catálogo de acciones bien hecho, no más features. |
| 29/08 post-mentores | **Arquitectura: pipeline de 5 capas, `docs/arquitectura.html`.** Generador → Motor determinístico (Detector → Localizador) → Agente RAG → Playbook + templates → Entrega. Regla: **lo determinístico calcula, la IA entiende**. El motor NO usa LLM. — equipo (pizarrón) | La detección y la localización tienen que ser correctas en vivo (trial by fire) → no pueden depender de un modelo. El LLM interpreta y redacta, no busca la causa. |
| 29/08 post-mentores | **Capa IA en 2 etapas distintas.** (1) **Agente RAG**: interpreta el output estructurado del motor contra una base de conocimiento en archivos `.md` (reglas de negocio de pagos), saca conclusión + severidad + categoría de acción. (2) **Playbook**: catálogo propio de acciones de remediación + templates; el LLM elige la que aplica y la redacta. — equipo | Meter todas las reglas de negocio en el prompt es complejidad y ruido; la base `.md` las mantiene ordenadas y editables. El catálogo propio da control sobre la calidad de la acción recomendada. |
| 29/08 post-mentores | **La base de conocimiento `.md` es estática** — la escribimos nosotros antes del evento, no se toca en la demo. Lo que varía en el trial by fire es el incidente que inyecta el juez, no las reglas. — equipo | Editar reglas en vivo es frágil y no suma; el valor a demostrar es que el sistema diagnostica un incidente no ensayado. |
| 29/08 post-mentores | **Entrega dual:** dashboard (la torre de control + panel de inyección del juez) **y** un canal ejecutivo tipo Slack/WhatsApp para el resumen en una línea. — equipo | El dashboard es lo que se opera en el trial by fire; el canal es el guiño al Payments Concierge de Yuno y cubre el bonus de "dos audiencias". |
| 29/08 post-mentores | **Acción recomendada en la demo: se muestra + botón "aplicar (simulado)"**, marcado claramente como simulación. El challenge **diagnostica, no ejecuta** — el loop de vuelta al generador existe solo para la demo. — equipo | Mostrar el efecto de la acción sobre el stream es vistoso; el marcado explícito evita que el jurado diga "esto era diagnosticar, no remediar". |
| 29/08 post-mentores | **Hito: mini demo end-to-end a las ~2 h** (todo falso: generador emitiendo, detector disparando sobre una caída hardcodeada, explicación stub, dashboard mostrando el incidente). — recomendación de mentor | Fuerza a tener el esqueleto conectado temprano y a atacar el spike de riesgo primero. Alcance exacto a confirmar con el mentor. |
| 29/08 producto/UX | **Dos superficies, una sola experiencia:** landing pública en `/` y panel operacional en `/control-tower`, compartiendo marca, lenguaje y componentes. La landing vende la promesa; el panel demuestra y opera el diagnóstico. — equipo / Juani | Separar persuasión de operación evita convertir la landing en un dashboard ilegible o el panel en una pieza de marketing. El CTA principal conecta ambas superficies y abre la demo real. |
| 29/08 producto/UX | **Autoridad visual y de marketing: Payments Concierge de Yuno.** Base negra/blanca, tipografía Geist, grilla técnica, titulares grandes y acento azul-violeta reservado. No se copia la página; se construye una extensión reconocible de la familia Yuno. — Juani | Hace que el prototipo parezca una capacidad integrable a Yuno y no una marca paralela. Nuestro diferencial visual y narrativo será la evidencia operacional, no decoración de IA. |
| 29/08 producto/UX | **La demo comienza con un stream sintético controlado desde la interfaz:** estados `READY / RUNNING / PAUSED / COMPLETE`, acciones `Start live stream / Pause / Reset` y badge persistente `SIMULATION MODE`. — equipo / Juani | Permite iniciar la secuencia sin teclado, repetir fixtures de aprobados y rechazados y ser transparentes con el jurado sobre el origen de los datos. |
| 29/08 producto/UX | **Composición del panel:** Command Center como vista principal y Incident Detail como profundización. La comparación temporal, los incidentes priorizados y el dinero en riesgo son el primer nivel; evidencia, ownership, confianza y acción recomendada son el segundo. — equipo / Juani | Conserva la complejidad útil de una herramienta operacional en tiempo real, pero sostiene una jerarquía clara para que el jurado entienda el diagnóstico en segundos. |
| 29/08 producto/UX | **Nombre del producto: Centinel**, con esa ortografía exacta. Descriptor: `Payment operations control tower`. — Juani / equipo | Es breve, pronunciable y expresa vigilancia permanente sin convertir el nombre en una explicación funcional larga. |
| 29/08 producto/UX | **Composición aprobada: A para Command Center + C como vista separada de investigación.** A es el inicio operacional; `Why this diagnosis?` navega a C. — Juani | A comunica estado y tiempo real de inmediato. C ofrece la profundidad necesaria para contrastar merchant/provider logs sin sobrecargar la pantalla principal. |
| 29/08 producto/UX | **Toda superficie de producto, landing, demo y pitch se presenta en inglés.** Los documentos internos del harness permanecen en español. — Juani / equipo | Cumple el formato del evento y evita mezclar idiomas frente al jurado, sin empeorar la velocidad de coordinación interna. |
| 29/08 producto/UX | **Contrato de datos UI:** Centinel consume `transactions`, `baseline_profile` e `incidents` mediante un adapter tipado; los cuatro ejes localizan y banco/código/latencia actúan como evidencia. Los IDs `BIGSERIAL` se serializan como strings. — equipo | Evita acoplar la interfaz al SQL, conserva precisión de IDs y separa dimensiones de diagnóstico de la evidencia que lo sustenta. |
| 29/08 integración | **Ventana operacional por defecto: últimos 60 s contra baseline contextual de 14 días.** La vista histórica de 2 h queda como exploración, no como hot path de detección. La demo usa entidades de PagoTotal y códigos ISO 8583 del plan de datos. — Juani / Pena | Alinea el frontend con el ring buffer, el forecast y los endpoints reales; elimina códigos y nombres mock que el backend nunca produciría. |
| 29/08 frontend | **La secuencia visual preserva silencio saludable antes de revelar incidentes; Pause congela el estado y Reset vuelve a healthy.** Los selectores de ventana/scope quedan explícitamente fijos hasta conectarlos al backend. `/demo-control` permanece fuera de `frontend/` porque el plan de ejecución lo asigna a Samo. — Juani | Evita que READY contradiga al gráfico, hace la demo reproducible y respeta las fronteras de trabajo del equipo sin presentar controles decorativos como terminados. |
| 29/08 producto/IA | **Firma de marca: `Centinel by toons`.** La configuración en lenguaje natural sólo crea borradores estructurados: validación, replay y aprobación humana son obligatorios. Diagnóstico, explicación, recomendación, decisión y resultado se guardan separados. — Juani | Distingue autoría de la referencia Yuno y evita que el LLM altere silenciosamente criterios críticos o borre el rastro entre evidencia y acción. |
| 29/08 frontend | **El product preview de la landing participa del flujo normal:** `headline → explicación → CTAs → dashboard`; sólo invade de forma controlada la sección siguiente. — Juani | Posicionarlo desde el borde inferior con altura implícita hacía que el dashboard subiera y tapara CTAs cuando crecían sus métricas. El flujo explícito elimina ese acoplamiento y mantiene la jerarquía en distintas alturas de pantalla. |
| 29/08 producto/UX | **Centinel deja de presentarse como dashboard y pasa a una investigación guiada:** `Observe → Detect → Validate → Diagnose → Compare/Explain → Decide → Monitor`. `Centinel Copilot` vive como rail contextual del incidente y una comparación conversacional forma parte del demo path P0. — Juani | La ventaja no es mostrar más métricas, sino conducir desde la anomalía hasta una decisión defendible. El Copilot interpreta agregados y evidence IDs producidos por el pipeline; no calcula sobre transacciones crudas ni modifica reglas. Si OpenAI falla, la misma evidencia se presenta con una respuesta estructurada determinística. |
| 29/08 datos | **T4 carga los 90 min disponibles del fixture (351.000 filas), sin fabricar los 30 min faltantes.** — Pena | T3 fija un fixture determinístico de 90 min, mientras T4 menciona 2 h/~470k. El loader toma hasta 2 h relativas al máximo del fixture; extenderlo exige cambiar T3 y renovar el handoff a Luca. |
| 29/08 datos | **T6 usa un reloj UTC monótono para filas `live` y conserva el tiempo de simulación del fixture, que se repite en cada vuelta, solo en el ring buffer.** — Pena | Evita que el fixture fechado el 24/08 congele `max(created_at)` al loopear y mantiene cada tanda sana alineada con su baseline contextual hasta que T9 lo re-ancla. |
| 29/08 datos | **El inyector y la evidencia de T7/T8 degradan probabilidades dentro del replay y tienen fallback al ring buffer.** — Pena | El jurado puede crear slices válidos sin JSON crudo y el guión mantiene cubo, códigos de rechazo, costo y reset aunque PostgreSQL local no esté disponible. |
| 29/08 datos | **El reanclaje de T9 conserva semilla y baseline; sólo traslada timestamps del fixture al momento del pitch.** — Pena | Se mantiene la reproducibilidad del escenario y el forecast sigue saliendo del perfil agregado por hora y tipo de día. |
| 29/08 frontend/UX | **La investigación no navega a una pantalla `/incidents/:id`: abre un workspace inline dentro de `/control-tower`.** El Command Center permanece como contexto visual detrás y la URL no cambia. — Juani | Evita romper la continuidad justo cuando aparece una alerta y permite contrastar métricas, cola, evidencia y conversación como partes de una sola sesión operacional. La ruta vieja se normaliza a `/control-tower`. |
| 29/08 frontend/UX | **Los CTA de la landing usan una transición de entrada de 1,45 s antes de abrir `/control-tower`.** La animación muestra el payment path `Merchant → Yuno → Provider` sincronizándose y respeta reduced motion. — Juani | Convierte el cambio de superficie en parte del relato de producto y evita un salto seco entre marketing y operación. La metáfora de arquitectura reemplaza al objeto orbital genérico para que el movimiento también explique el producto. |
| 29/08 producto/UX | **La cabecera operacional separa `Live Monitoring` de `Explore`.** Explore admite dos rangos históricos personalizados y scope por dimensiones; una consulta natural puede compilarse a filtros visibles antes de `Run analysis`. Centinel también podrá proponer criterios de alerta, pero sólo como `PolicyDraft` gobernado; queda abierta la interacción `LLM vs inputs`. — Juani | Separa detección automática, análisis ad hoc y configuración de políticas para que el gráfico tenga una pregunta explícita. Mantiene potencia exploratoria sin permitir que una instrucción ambigua altere el detector ni comprometa el trial by fire. |
| 29/08 pitch | **Formato de presentación: 2:00 de producto/introducción + 5:00 de demo en vivo.** La demo reserva 1:15 final para trial by fire; `Explore + PolicyDraft` tiene un máximo de 0:40 y es el primer bloque a cortar si el ensayo supera siete minutos. — Juani | Protege los resultados obligatorios del challenge y evita que una capacidad P1 consuma el tiempo destinado a evidencia, incidentes simultáneos, incertidumbre y prueba del juez. |
| 29/08 frontend/UX | **Los controles de `Live Monitoring` pasan a ser interactivos para la vista:** observed window (`60 s / 5 min / 15 min`), referencia (`14-day / previous period / contract threshold`) y scope predefinido. Cambian KPIs, título y contexto del gráfico; no reconfiguran la política del detector. `Reset` restaura la vista default. — Juani | Permite consultar el stream sin entrar a Explore y elimina controles decorativos, manteniendo separada la navegación visual de la configuración gobernada de alertas. |
| 29/08 post-mentores | **Abiertas** (sin decidir): cardinalidades y throughput del generador · números de latencia (`< 45 s` alerta / `< 90 s` diagnóstico, propuestos) · overlap permitido entre los 2 incidentes. **Cerradas:** nombre Centinel y cubo `merchant × provider × method × country`; banco/código quedan como evidencia. | Las abiertas se cierran en las primeras horas del sábado; las cerradas ya tienen contrato de producto y datos. |
| 29/08 integración | **La capa de explicación es de Samo:** recibe el `EngineOutput` determinístico de Luca, selecciona una acción desde catálogo, obtiene el costo de `money_lost` de Pena y sólo usa Pydantic AI sin tools para redactar en inglés. | Mantiene detección, localización, clasificación y decisión de acción auditables; si OpenAI o datos no están disponibles, templates y fallback monetario conservan la demo. |
| 30/08 explain | **Capa de explicación implementada y verificada** en `backend/explain/` (contratos en `backend/contracts.py`, API `POST /api/agent/explain` en `backend/main.py`). El **`headline` es determinístico** (template con la plata + el slice), no lo escribe el LLM. El LLM (`gpt-4o`, temp 0, timeout 10 s) solo redacta `executive` / `operations` / `action_rationale`. Sin key o sin `pydantic-ai` → fallback Jinja con la misma evidencia y acción. — Samo | El LLM erraba el `headline` (ponía la acción en vez de la situación+plata). Tono verificado contra `08-product-ux-gtm.md` §11 en los 3 fixtures: "the evidence indicates", sin alarmismo, sin cuantificar cuando no hay costo. |
| 30/08 explain | **Prioridad de incidentes = `impacto × alcance × persistencia × confianza × criticidad_merchant`**, con `impacto` relativo al máximo del batch (no cap fijo). Cada componente queda expuesto en la respuesta. — Samo | Con cap fijo a $10k, dos incidentes grandes topaban en 1.0 y la plata dejaba de diferenciar; el de más plata quedaba segundo, invirtiendo la narrativa del pitch. |
| 29/08 spike detector | **Primer corte implementado por país: CUSUM binomial unilateral por slice + baseline horario opcional + estado independiente por país.** Los thresholds quedan provisionales hasta calibrarlos contra el replay final. | El approval rate nace de éxitos/intentos binomiales: usar ambos conteos incorpora el volumen y evita tratar igual una caída con 5 intentos que otra con 500. Un estado por país permite abrir, priorizar y resolver dos incidentes simultáneos sin mezclar identidades. |
| 29/08 contrato diagnóstico | **`diagnosis_category` se decide de forma determinística en `/backend/core`, después del localizador y antes de RAG/playbook.** Se implementa como módulo separado del detector; combina slice, decline shift, evidencia de issuer y confianza. El LLM puede redactar, pero no elegir ni cambiar la categoría. | Mantiene el trial by fire reproducible y establece una frontera clara: el core detecta, localiza y clasifica; el playbook selecciona la acción; el LLM comunica. Un mapeo de código aislado no alcanza porque el mismo código puede implicar causas distintas según la forma del slice y los segmentos de control. |
| 29/08 integración core-datos | **La entrada canónica del core es `get_cube(60s)` con 81 hojas; `process_events` queda como adaptador offline/fallback. Issuer y decline code entran después, vía `get_evidence(filters, 60s)`.** | Evita duplicar el agregador de Pena en el hot path, conserva una forma de probar el motor aisladamente y limita el costo de evidencia a los slices que el localizador realmente seleccionó. |
| 29/08 incidentes superpuestos | **El localizador usa residualización multiplicativa: luego de elegir una causa, descuenta su ripple del déficit y del baseline todavía disponible antes de buscar la siguiente.** | Permite separar dos incidentes simultáneos aunque compartan país y hojas del cubo, sin convertir issuer ni decline code en dimensiones y sin pedirle al LLM que resuelva la ambigüedad. |
| 30/08 contrato core-explain | **La taxonomía determinística del core permanece canónica. La capa explain traduce `issuer_over_declining → issuer_declining` y `payment_method_outage → method_degradation` sólo para seleccionar templates/playbooks, y agrega acciones propias para `merchant_integration_error` y `merchant_configuration`.** | Evita cambiar el veredicto auditable emitido por el motor y permite que todas sus categorías soportadas lleguen a una recomendación determinística en la capa de Samo. |
| 30/08 producto/docs | **El README público adopta la narrativa del pitch y funciona también como índice verificable:** problema → Payment Truth → investigación guiada → arquitectura → evidencia en código → estado real → ejecución local. Declara por separado lo implementado y la integración pendiente. — Juani / Codex | Permite que jurado, mentor o desarrollador entienda el valor en menos de un minuto y pueda comprobar cada promesa sin confundir prototipo, datos sintéticos o roadmap con capacidad productiva. |
| 30/08 e2e | **Primer e2e real conectado, sin esperar a la capa de datos de Pena.** `scripts/e2e_check.py` corre `core (motor real) → bridge → explain` sobre los escenarios de `run_core_demo` (healthy/provider/dual/overlap/unseen/weak) — imprime `E2E CHECK: PASS`. El bridge `core.EngineOutput.to_dict()` → `contracts.EngineOutput` es directo: solo se emiten incidentes en `detected`/`insufficient_evidence`. — Samo | El eslabón motor↔explain hoy solo lo unían los 3 fixtures a mano; ahora está probado con salida real del engine. |
| 30/08 frontend/UX | **La torre se destila al relato `Watch → Validate → Diagnose`; `Explore + PolicyDraft` sale del pitch cronometrado.** Se eliminan navegación decorativa, selectores de vista redundantes y el detalle duplicado. La evidencia aparece por disclosure progresivo en un workspace inline con Operations / Executive y Copilot contextual. — Juani / Codex | El harness ya definía Explore como primer corte. La interfaz debe acompañar lo que se dice, no obligar al jurado a descifrar un dashboard mientras ocurre la detección. El tiempo recuperado se entrega a incidentes simultáneos, incertidumbre y trial by fire. |
| 30/08 frontend/integración | **Frontend modular en React + CSS authored, no Tailwind.** La experiencia principal consume sin cambios los endpoints debug existentes (`reset`, `inject`, `tick`); `/e2e` se conserva como superficie técnica. Landing y torre incluyen toggle persistente EN/ES, con inglés como idioma público de demo. — Juani / Codex | Evita una migración de estilos de último momento, mantiene el backend bajo propiedad de Samo/Pena/Luca y permite ensayar en español sin mezclar idiomas frente al jurado. |
| 30/08 frontend/UX | **La cola de incidentes scrollea dentro de su propia columna y conserva su header visible; el workspace mantiene el selector EN/ES en una sola línea junto al cierre.** — Juani / Codex | Permite revisar todos los incidentes simultáneos sin perder el gráfico ni desplazar la torre completa, y corrige el desborde del toggle en viewports angostos. |
| 30/08 frontend/UX | **La comparación P0 vuelve como `ComparisonWorkspace`, no como el viejo Explore.** Consume `/api/cube?window_s`, compara baseline contextual o dos ventanas móviles sobre scope explícito y conserva múltiples consultas en la sesión. Rangos históricos `from/to` quedan bloqueados hasta que exista contrato backend. También se exponen alternativas, missing evidence, parámetros de acción, componentes de prioridad y el fixture de evidencia insuficiente. — Juani / Codex | Recupera US-04/US-10 y la mitad investigativa del demo sin reintroducir un dashboard cargado ni fingir capacidades históricas. Mantiene la frontera: agregados calculan, IA explica, humano decide. |
| 30/08 e2e | **Panel de debug `/e2e` en el frontend Vite** (`src/e2e.tsx` + `src/api.ts`), consume `POST /api/agent/explain` real y muestra todos los campos del `Diagnosis` + prioridad + JSON crudo + log de llamadas. `backend/main.py` gana CORS (`localhost:5173` + `FRONTEND_ORIGIN` para deploy); Vite proxya `/api`. No toca `Landing`/`CommandCenter` de Juani. — Samo | Ver y debuggear el flujo entero en el navegador contra el backend real. El stream en vivo / inyector entran cuando Pena termine T4-T8; la UI final (CommandCenter) reusa el mismo `api.ts`. |
| 30/08 e2e | **Fixture `pix_method_outage`** (`backend/fixtures/`) + escenario `pix_br` en `e2e_check.py`: método `pix` en BR cae de 96% a 70%, código 96 dominante → `payment_method_outage` → acción `review_payment_method` (owner: merchant). El motor real lo clasifica bien. — Samo | No había fixture que ejercitara el path método/`method_degradation`; sirve para probar el alias de categoría y una caída de un método, no de un proveedor. |
| 30/08 backend | **Logger legible del backend** (`backend/logging_setup.py`, stdlib `logging`, toggle `CENTINEL_LOG`). Una línea por paso del pipeline: request → parse → por incidente (categoría/estado, acción elegida del catálogo + alias, conteo de evidencia, costo + si es fallback, resultado del LLM con motivo) → prioridad → respuesta con latencia. — Samo | "Ver y entender el flujo de procesamiento" en la consola sin abrir el debugger. |
| 30/08 e2e | **Pipeline stream de debug** (`backend/debug_pipeline.py` + rutas `/api/debug/stream/{reset,tick}` y `/api/debug/inject`). El botón ▶ Start del panel `/e2e` resetea el motor, inyecta un preset (PIX BR 70% / Adyen BR / emisor MX) y avanza una ventana de 60 s cada 2,5 s. **Cada ventana narra la cadena completa en español por la consola del backend:** `[GENERADOR]` cubo cargado → `[INYECTOR]` celdas degradadas, approval antes→después → `[MOTOR]` localizador (score/ripple/coverage/déficit) + clasificador (categoría/confianza/señales) + estado (validating→diagnosing→detected) → `[BRIDGE]` core→contracts → `[EXPLAIN]` acción/costo/LLM → `[PRIORIDAD]` → `[API]`. Los mismos pasos vuelven en `steps[]` y se ven en el panel. — Samo | El pedido era ver correr y debuggear la arquitectura entera desde el frontend, con logs en español que expliquen cómo se transformó la data en cada eslabón. `debug_pipeline.py` es descartable: lo reemplaza el stream + inyector reales de Pena (T4-T8). |
| 30/08 integración | **Loop de diagnóstico vivo** (`backend/diagnosis_loop.py`, montado en el `lifespan`): un `CentinelEngine` stateful corre cada ~5 s sobre el cubo de 60 s del ring buffer real de Pena → bridge → `diagnose()`. `GET /api/diagnosis` devuelve el snapshot + `log_tail`. `POST /api/diagnosis/reset` resetea el motor (el frontend lo llama junto con `/api/demo/reset`). Se borró `debug_pipeline.py` y las rutas `/api/debug/*`. — Samo | Cierra el eslabón que faltaba (`get_cube → motor → explain`); la narración en español ahora sale de datos vivos, no del parquet. El costo es el real de `money_lost`. |
| 30/08 integración | **Postgres es opcional.** `get_evidence`/`money_lost` van **ring-first** (si el replayer está, no tocan PG); `db.connect()` lleva `connect_timeout=2`. La demo corre 100% sin base — ring buffer (memoria, 30 min) + parquet (baseline 14 días + fixture). PG queda como archivo frío de transacciones para auditoría. — Samo / Pena | Sin timeout, un 5432 filtrado colgaba `/api/evidence` y el loop ~150 s. `backend/data/cube.py` y `backend/db.py` tocados — avisado a Pena. |
| 30/08 integración | **CommandCenter cableada a datos vivos** vía `frontend/src/{useLive.ts,live.ts}` + `api.ts` (SSE `/api/stream` + poll `/api/diagnosis`). `main.tsx`: `useLive()` con fallback al mock, botones de inyección en la barra de stream, `Reset` resetea el backend. `/e2e` migrado a los endpoints reales de Pena. **Follow-up:** el bloque "Selected incident" y el `SignalChart` siguen mock; `/demo-control` como panel del juez. — Samo | Diff mínimo en el archivo de Juani (hook + swap de la lista + 2 botones), sin reescribir su markup. |
| 30/08 frontend/UX | **El Command Center separa tres conceptos:** filtros dimensionales para definir qué cruce se inspecciona, simulación marcada como demo-only y comparación temporal en un workspace aparte. Desaparecen `Prepared / Judge mode`; el jurado usa el mismo panel de dimensiones que operaciones. — Juani / Codex | Evita que un selector de alcance parezca crear mágicamente el incidente y hace defendible el trial by fire: elegir scope no altera evidencia, mientras simular sigue siendo una acción explícita y auditable. |
| 30/08 frontend/integración | **El stream principal se construye ventana a ventana desde SSE `/api/stream`; diagnóstico e incidentes llegan por polling de `/api/diagnosis`.** La tabla muestra actividad agregada de stream/scope/detector/diagnóstico y la cola vive en una pestaña propia. No se inventan transacciones individuales: el `log_tail` del loop vivo es la traza visible. — Juani / Samo / Codex | Alinea la experiencia con el pipeline real recién integrado y mantiene la secuencia `watch → validate → diagnose` verificable sin depender de las rutas debug eliminadas. |
| 30/08 frontend/arquitectura | **`frontend/src/main.tsx` vuelve a ser sólo el entrypoint; `App`, Landing y CommandCenter modulares quedan como única implementación canónica.** La integración viva se consume desde `pages/CommandCenter.tsx` mediante `useLive`, no desde una segunda aplicación monolítica duplicada en `main.tsx`. — Juani / Codex | El merge de integración había reintroducido ~640 líneas legacy paralelas a los componentes modulares. Mantener dos torres hacía que las correcciones visuales y el contrato vivo avanzaran por caminos distintos y podía romper el build por nombres duplicados. |
| 30/08 frontend/UX | **La cola priorizada vuelve a ocupar el rail derecho permanente del Command Center.** Stream, gráfico y traza quedan a la izquierda; cada incidente muestra impacto y score y abre inline su diagnóstico + playbook. En mobile las dos superficies se apilan. — Juani / Codex | Detectar y priorizar son parte de la misma escena operacional: esconder incidentes detrás de una pestaña cortaba el relato, desperdiciaba la columna reservada y obligaba a perder el contexto del stream justo antes de investigar. |
| 30/08 frontend/UX | **Se elimina la barra `Live stream / Scope` entre KPIs y evidencia.** El scope se expresa como resumen `Applied filters` dentro de `Detection filters`. — Juani / Codex | No existen dos vistas que navegar: stream y cola son simultáneos. El cruce dimensional describe la configuración activa del filtro, no una sección nueva ni un contexto global separado. |
| 30/08 frontend/i18n | **El toggle EN/ES cubre toda la experiencia pública, incluida la copia dinámica del diagnóstico.** El backend sigue emitiendo su contrato canónico en inglés; el frontend deriva la versión española desde categoría, scope, evidencia, costo y acción estructurados. IDs, códigos, marcas y nombres propios no se traducen. — Juani / Codex | Traducir sólo labels dejaba headline, evidencia y playbook en inglés. Derivar la copia desde hechos estructurados mantiene el cambio instantáneo, evita otro llamado al LLM y conserva exactamente el mismo diagnóstico en ambas audiencias. |
| 30/08 frontend/UX | **La siguiente iteración prioriza verdad funcional antes que polish:** `Aplicar vista`, `Pausar`, Start/Reset y Copilot deben comunicar exactamente el efecto que ejecutan. Accesibilidad, lectura del gráfico y limpieza visual siguen después. — Juani / Codex | La auditoría funcional encontró controles convincentes cuyo efecto real es más limitado que su copy. En el pitch, una promesa falsa daña más la confianza que un detalle visual incompleto. Hallazgos y criterio de salida en `docs/13-ux-functional-audit.md`. |
| 30/08 frontend/UX | **El gráfico vivo muestra volumen por snapshot como velas apiladas:** aprobadas en verde, rechazadas en rojo y aprobaciones esperadas como línea. `tx_count` es acumulado, por lo que el frontend grafica su delta entre snapshots. Los filtros configuran exclusivamente el alcance de la señal simulada; no afirman filtrar KPIs globales. — Juani / Codex | El volumen hace visible que los registros están entrando y permite distinguir una caída de conversión de un cambio de tráfico. Usar el contador acumulado directamente dibujaba una falsa tendencia creciente; convertirlo a delta mantiene la representación honesta sin cambiar backend. |
| 30/08 frontend/UX | **La evidencia del incidente se ubica antes del gráfico y excluye tráfico sano y plumbing interno.** Sólo muestra señal inyectada, slice, volumen/approval, localización, clasificación y estado; conserva los ocho eventos relevantes más recientes. Las velas reducen su ancho para separar mejor cada snapshot. — Juani / Codex | El log completo competía con la visualización y hacía difícil encontrar las filas que justifican el incidente. El detalle de LLM, bridge y playbook ya existe en la investigación, por lo que repetirlo en la torre agregaba ruido. |
| 30/08 frontend/UX | **Corrección de alcance del log:** la tabla superior muestra todos los snapshots entrantes evaluados, produzcan o no un incidente. Cada fila conserva volumen, aprobadas, rechazadas, approval observado/esperado y una lectura visual `Expected / Watch / Strong deviation`; se elimina solamente el ruido del plumbing interno. — Juani / Codex | “Limpiar logs” significa hacer escaneable el flujo completo, no ocultar casos saludables. Centinel necesita demostrar tanto que detecta anomalías como que observa tráfico normal sin disparar alertas. |
| 30/08 frontend/UX | **El detalle del incidente separa explicación humana de trazabilidad técnica.** Operations responde en pocas líneas causa raíz, alcance, inicio, caída e impacto; mantiene visibles `What changed` y `Why this cause`. Wilson, códigos, emisores y señales quedan en `Full evidence trail`. Executive conserva una línea con dinero y próximo paso; alternativas y datos faltantes nunca se colapsan. — Juani / Codex | La consigna exige explicación entendible y evidencia visible, no un párrafo que concatene todas las pruebas. La divulgación progresiva permite comprender el incidente en segundos y auditarlo cuando hace falta, usando siempre el mismo bundle estructurado. |
| 30/08 frontend/UX | **La cola de incidentes es una memoria acumulativa de la sesión.** Cada snapshot actualiza incidentes ya conocidos por `incident_id` y agrega los nuevos sin eliminar los anteriores; sólo `Reset` vacía el stack. La cola conserva la prioridad explicable y distingue señal presente de registro retenido. — Juani / Codex | La demo debe separar y priorizar dos incidentes simultáneos. Reflejar únicamente el último snapshot hacía desaparecer evidencia ya detectada y rompía la historia operacional cuando las señales no se emitían en la misma ventana. |
| 30/08 producto/UX | **`Compare periods` produce un hallazgo comparativo, no declara automáticamente un incidente.** Consulta cubos estadísticos para ambas ventanas, muestra muestras, approval, delta, pérdida estimada e identifica el cruce con mayor degradación. Sólo recomienda una acción humana cuando la muestra y el contraste la sostienen; ante evidencia insuficiente pide ampliar ventana o acotar alcance. — Juani / Codex | Comparar dos porcentajes sin diagnóstico ni próximo paso no reduce el trabajo del operador. A la vez, una comparación exploratoria no cumple por sí sola las validaciones de persistencia del detector y no debe inventar una causa raíz ni ejecutar remediación. |
| 30/08 datos/UX | **El gráfico de tasa muestra una banda de predicción al 95% alrededor del esperado, no un número puntual.** `banda = p ± 1.96·√(p(1−p)/n)`, con `p` = tasa esperada del baseline estacional y `n` = intentos de la **ventana de 60 s** (`overview.attempts`), no del punto. Se calcula en el cliente; **no cambia detección ni localización**. Con `n≈3.900` y `p≈0,874` la banda mide ±1,04 pts y un incidente `provider_br` la excede por 22 desvíos. — Pena | Sin banda, el jurado tiene que decidir a ojo si un hueco entre observado y esperado es señal o ruido. Es la banda gris del anomaly detection de Datadog y hace visible la certeza estadística que el motor ya usa internamente. Va en espacio de tasa y no sobre el gráfico de volumen porque ahí mediría ~3 px; y usa el `n` de la ventana y no el del punto (~65) porque la serie graficada es la tasa móvil de 60 s — con el `n` del punto la banda daría ±8 pts, ocho veces más ancha, mintiendo en la dirección cómoda. |
| 30/08 frontend/visual | **El gráfico de volumen agrupa los snapshots en velas visuales de 6 segundos, sin eliminar filas del log.** Cada vela suma intentos, aprobadas, rechazadas y referencia esperada de su bucket; usa gradientes translúcidos, borde y sombra sutil para separar capas sobre el fondo oscuro. — Juani / Codex | Treinta barras finas producían ruido y parecían un histograma genérico. Una cadencia menor mejora la lectura durante el pitch, mientras la tabla conserva la frecuencia completa y evita perder evidencia operacional. |
| 30/08 frontend/UX | **La tasa de aprobación contra su rango esperado precede al volumen y recibe mayor zoom.** El dominio mínimo baja de 4 a 2,6 puntos, el padding de 0,8 a 0,35 y la superficie crece; ante una caída grande la autoescala conserva todos los valores. Las velas quedan después como contexto de volumen. — Juani / Codex | La pregunta primaria es si la conversión salió del comportamiento esperado; el volumen ayuda a validar la muestra, pero no debe competir con esa señal. El zoom permite leer variaciones normales y el cruce de la banda sin deformar el intervalo estadístico. |
| 30/08 backend/notify | **Alerta real de Slack por incidente `supported` con acción accionable (no `monitor`).** Nuevo módulo `backend/notify_slack.py`, llamado sólo desde `diagnosis_loop._tick()` — nunca desde `explain.build.diagnose()`, para que `/api/agent/explain` y `explain/selfcheck.py` sigan sin efectos secundarios. Dispara una vez por `incident_id` (dedup en memoria, `set`), inmediato en el primer tick `supported`, sin flag de "armado" adicional — sólo `SLACK_WEBHOOK_URL` en `.env`. Mensaje: headline determinístico + `operations` completo + `incident_id` + "simulation only". Reintenta en el próximo tick si el POST falla; sólo marca notificado tras un 2xx. — Samo | El canal era hasta ahora un mock visual (#18). `CentinelEngine.reset()` reinicia `_incident_sequence` a 0, así que el dedup se limpia junto con `diagnosis_loop.reset()` — si no, un incidente nuevo en la corrida real podría heredar un `incident_id` viejo ya "notificado" y quedar mudo en el peor momento. Meter el hook adentro de `diagnose()` habría hecho que cada click de prueba en `/e2e` o cada corrida del self-check disparara un mensaje real. |
| 30/08 frontend/notify | **La entrega a Slack se muestra en la UI como un toast animado (`components/SlackAlertToast.tsx`), disparado por confirmación real del backend, no por un timer decorativo.** `notify_slack.recent()` expone sólo las alertas con 2xx confirmado; el loop las publica en `slack_alerts` dentro de `GET /api/diagnosis` y el toast anima la más nueva por `incident_id`. Logo de Slack en 4 colores, 8 s en pantalla, respeta `prefers-reduced-motion`. El canal `#dev-alerts` está hardcodeado en el texto del toast. — Samo | El canal es el remate del pitch ("antes de que salga en X.com") y necesitaba ser visible en la pantalla que el jurado mira, no sólo en Slack. Atarlo a la confirmación real evita el problema que marcó la auditoría funcional (`docs/13-ux-functional-audit.md`): un control que promete más de lo que ejecuta. Medido en vivo: 12–25 s entre la inyección y la alerta entregada, porque el incidente recorre validating → diagnosing → detected antes de ser `supported`. |
| 30/08 demo/timing | **El warm-up del ring se resuelve en el guión, no en el código: `Start live stream` se aprieta al empezar el bloque de producto del pitch, no antes de inyectar.** `Start` llama `/api/demo/reset` → `replayer.reset_window()` → `ring.clear()`, así que el cubo de 60 s se vuelve a llenar desde cero. Medido en vivo: 0 → 325 → … → 3.900 intentos en 12 ventanas (~60 s). Se evaluó y **descartó** desacoplar `Start` de `Reset` (que `Start` no vacíe el ring) y el pre-llenado automático del buffer. — Samo | Los 2 minutos de producto del `script-demo.md` cubren de sobra los 60 s de warm-up, así que el problema desaparece sin tocar una línea. Las dos alternativas tocaban `replayer`/`routes.py` (dueño: Pena) a horas del evento y sobre el camino crítico de la demo: cambiar el significado de `Reset` o el pre-llenado del ring introduce más riesgo del que elimina. Nota: inyectar durante el warm-up igual funciona (probado: diagnóstico en 12 s con el ring al 27%), pero las ventanas de bajo volumen suman entradas `insufficient_evidence` a la cola acumulativa, que contradicen la línea "Noise is not an incident" del paso 1. Precondición registrada en `pitch/demo-path.md`. |
| 30/08 frontend/fix | **Bug del toast de Slack: quedaba fijo en pantalla para siempre.** El `useEffect` tenía `shown` en las dependencias, así que al guardar el toast el efecto se re-ejecutaba, el cleanup mataba el `setTimeout` del fade y el early-return no programaba uno nuevo. Ahora depende sólo de `incident_id` y además desmonta el nodo a los 8,5 s (antes quedaba con `opacity: 0` tapando clicks). — Samo | Verificado en vivo: aparece, se desvanece a los 8 s y libera la cola de incidentes. Un toast permanente arruinaba la pantalla que el jurado mira el resto de la demo. |
| 30/08 frontend/inject | **La señal inyectada ahora expira: selector de `Duración` en el panel (60 s / 2 min / 5 min / hasta detenerla), default 2 min.** El injector ya soportaba `duration_s` desde T7 pero el frontend nunca lo mandaba, así que toda señal inyectada quedaba activa para siempre. — Samo | Probado: `INJ-0005` con `duration_s=60` salió solo de la lista de activos; el preset sin duración seguía activo. Aclaración para el guión: al vencer, la señal **corta seco** (`status: expired`); la recuperación *gradual* del multiplicador es el otro camino, el de `mitigated_at` ("aplicar acción"). |
| 30/08 landing | **La landing pasa a usar los componentes reales del producto: el hero y el preview muestran `ExpectedBandChart` (banda de predicción 95%) con una serie determinística en `demo/heroSeries.ts`, en lugar del `SignalChart` de paths SVG hardcodeados con timestamps fijos `14:02:21`.** — Samo | La promesa pública mostraba un gráfico que el producto ya no tiene: el jurado veía una curva en la landing y velas + banda en la torre. Ahora la landing enseña exactamente lo que va a ver después, y la banda es el diferencial técnico más reciente. La serie es determinística a propósito (sin `Math.random`) para que no titile entre cargas. |
| 30/08 landing/UX | **Scroll reveal por sección con `IntersectionObserver` (`useReveal.ts`), con escalonado de los hijos y `prefers-reduced-motion` respetado.** El CSS oculta **únicamente** dentro de `.reveal-ready`, clase que agrega el propio script; sin JS, sin `IntersectionObserver` o con reduced motion la landing se ve completa. Un failsafe a los 4 s revela todo **sólo si nada se reveló** (señal de que el observer está roto), para no matar la animación de las secciones de abajo. — Samo | Regla 4 de `CLAUDE.md`: una animación de entrada mal implementada es la forma más fácil de dejar la landing en blanco en vivo. Atar el ocultamiento a una clase que pone el JS hace que el modo degradado sea "se ve todo sin animar", nunca "no se ve nada". |
| 30/08 backend/notify | **Qué incidentes disparan Slack, explícito:** SÓLO los que el clasificador determinístico pudo resolver — `diagnosis_status == "supported"` **y** una acción real del catálogo (`owner != "none"`). Los tres estados posibles son `supported`, `insufficient_evidence` y `unclassified` (`backend/core/models.py:25`); los dos últimos caen siempre en la acción `monitor` del catálogo y **no** notifican. — Samo | El canal de devs es el remate del pitch: si alerta por ruido de bajo volumen o por caídas que ninguna regla de dominio explica, deja de ser una alerta y pasa a ser spam — y contradice la promesa "Centinel no alerta por ruido". El gate es el mismo que usa la UI para declarar un incidente, así que pantalla y canal nunca se contradicen. |
| 30/08 explain/fix | **`unclassified` deja de decir "evidence still insufficient" en el headline.** Ahora dice `Unexplained approval drop in {slice} — no domain rule matched`. El texto viejo se usaba para dos estados distintos y en `unclassified` era falso. — Samo | Un caso real de la demo mostraba "evidence still insufficient" con **28 puntos de evidencia** listados abajo, y n=1105. La evidencia sobraba; lo que faltaba era una regla de dominio (`NO_SUPPORTED_DOMAIN_RULE`). Peor: cuanto más amplio el slice, más evidencia bruta acumula y más probable es que quede sin clasificar — el contador y el veredicto se movían en direcciones opuestas. Corregido en backend (`explain/build.py`) y en la versión española (`localization.ts`). |
| 30/08 frontend/UX | **`Detection filters` se separa en dos controles honestos: `Signal simulator` (inyección, subtítulo "Trial by fire") y `View filter` (filtro de vista).** El panel viejo se llamaba "filtros" pero no filtraba nada: sólo definía el scope de la señal a inyectar. — Samo / Juani | El nombre mentía y mezclaba dos features. El simulador ahora mapea de forma directa con el requerimiento de trial by fire del challenge, que es como se lo presenta al jurado. |
| 30/08 backend+frontend | **Filtro de vista real sobre KPIs y cola de incidentes, con su alcance declarado en pantalla.** `GET /api/overview` acepta las 4 dimensiones del cubo por query param (reusa el helper `_query_filters`); la cola se filtra en el frontend conservando los slices más amplios (dimensión en `null` contiene al filtro). **Los gráficos siguen globales y el panel lo dice explícitamente.** `issuer_bank` se ignora: no es eje del cubo. — Samo | Pedido concreto: poder ver estadísticas por proveedor. Verificado contra el generador — adyen 88,4% vs dlocal 84,7% sobre el mismo tráfico, que coincide con los `PROVIDER_APPROVAL_OFFSETS` de `world.py` (+0,012 / −0,010). Segmentar el ticker del replayer era el 80% del costo por el 20% del valor, así que en vez de filtrarlo a medias se declara su alcance: la alternativa (filtrar KPIs y dejar el gráfico global en silencio) es justo el tipo de control que la auditoría funcional marcó como el más caro en un pitch. |
| 30/08 frontend/UX | **El input del Copilot sube al tope del rail de diagnóstico** (tenía `margin-top: auto` y quedaba al fondo, fuera de vista al abrir el incidente). La respuesta y las sugerencias crecen debajo. — Samo / Juani | Al abrir un incidente lo primero accionable tiene que estar a la vista; el jurado no debería tener que scrollear el rail para encontrar dónde preguntar. |
| 30/08 frontend/UX | **El `Signal simulator` se muda a la columna derecha, arriba de la cola de incidentes, y el rojo (`--incident`) queda reservado para él; el `View filter` pasa a borde neutro.** — Samo / Juani | El simulador es lo único de la interfaz que **altera el mundo**: merece el color de alerta y vivir junto a lo que produce (los incidentes de la sesión). El filtro de vista sólo mira, así que compartir el rojo con él diluía la señal. Ubicarlo ahí también libera la toolbar, que ya tenía cinco controles. Detalle de implementación: el panel necesita `z-index` propio porque `.tower-incidents` crea contexto de scroll y el desplegable quedaba detrás del bloque "Trustworthy silence". |
| 30/08 copilot/IA | **El Centinel Copilot pasa a responder con LLM sobre el bundle de evidencia (`backend/explain/copilot.py`, `POST /api/copilot/ask`), con el keyword matching anterior como fallback.** Patrón: el frontend pinta **primero** la respuesta determinística (instantánea) y recién después la reemplaza con la del LLM; si falla, tarda o no hay API key, queda la determinística y no se nota nada. `gpt-4o-mini`, temperatura 0, timeout 6 s. Reusa las mismas `rules/*.md` que la capa de explicación vía la constante compartida `DOMAIN_RULES`. El frontend manda el `Diagnosis` completo, no un `incident_id`. Respuestas siempre en inglés. — Samo | El ruteo por palabras clave fallaba en cualquier pregunta no anticipada, y la interacción conversacional es P0 del demo-path. **Por qué el bundle y no el id:** `DIAGNOSES` sólo se llena desde `/api/agent/explain` (el loop vivo no lo alimenta) y la cola de la UI es acumulativa, así que un incidente viejo no se podría resolver por id. **Por qué reusar `DOMAIN_RULES`:** si el Copilot razonara con otras reglas podría contradecir en pantalla el diagnóstico que la misma base ya redactó. **Por qué inglés:** mantiene sin excepción la regla #41, y el pitch es en inglés por #27. Verificado en vivo: ante "is it safe to retry?" aplicó `rules/05-code-91.md` y citó los datos del incidente inyectado (7% → 67% de código 91); ante una pregunta de facturación devolvió `out_of_scope: true` sin inventar nada; y con el LLM caído conservó la respuesta determinística. |
| 30/08 frontend/UX | **Los tres bloques del workspace en vivo se leen como un pipeline numerado (1 log · 2 banda · 3 volumen) y su bajada explica CÓMO leer el gráfico, no cómo está implementado.** Los tres encabezados comparten ahora el mismo contenedor (64 px, mismo padding y borde); antes sólo `.pipeline-log > header` tenía uno y los de los gráficos eran texto suelto, así que el título se despegaba de su gráfico. Se elimina el contador `X/30 snapshots` duplicado. — Samo / Juani | El subtítulo de la banda decía "Prediction interval from the seasonal baseline and the 60-second sample size": describe la implementación y no le dice a nadie qué significa la franja gris. Ahora dice **"si la línea sale de la banda, la caída no es ruido estadístico"**, que convierte el gráfico en un argumento que el jurado entiende sin explicación hablada. Misma lógica en el de volumen: "una caída con poco volumen no es un incidente" justifica en una línea por qué el motor no alerta por ruido. |
| 30/08 frontend/UX | **Los contenedores de los gráficos pasan a `aspect-ratio: 920 / 190` en vez de una altura fija.** — Samo / Juani | Los SVG usan `viewBox="0 0 920 190"` sin `preserveAspectRatio`, así que el navegador aplica el default `xMidYMid meet`: centra el dibujo y reparte el sobrante. Con contenedores de 350 px y 290 px eso dejaba **~95 px muertos arriba y ~95 px abajo de cada gráfico** — el título quedaba despegado de su curva y el pipeline no entraba en una pantalla. Con `aspect-ratio` la caja mide exactamente lo que se dibuja (aire muerto medido: 0 px), el gráfico se pega a su encabezado y escala solo con el ancho de la columna en vez de depender de dos alturas fijas y una media query. |
| 30/08 frontend/fix | **Los dos desplegables de la torre dejan de pisarse: son mutuamente excluyentes, `Escape` cierra ambos, un panel abierto va a `z-index 60`, y el del simulador pasa a `position: fixed` anclado al borde inferior con scroll interno.** — Samo | Tres bugs encadenados, todos medidos con `elementFromPoint`. (1) El panel del filtro (`z-index 20`) quedaba **debajo** del simulador (`z-index 30`, que le puse yo al moverlo a la columna derecha): el click no llegaba al campo `Method`. (2) Con el simulador a media altura de la columna, su panel de 446 px arrancaba en y=500 sobre un viewport de 698: **los últimos 4 campos y el botón `Inject test signal` caían fuera de pantalla** — el control central del trial by fire era inalcanzable. (3) Como vive dentro de `.tower-incidents` (`overflow-y: auto`), abrirlo hacia arriba lo habría recortado, así que `fixed` es la única salida limpia; verificado que ningún ancestro tiene `transform`/`filter` que rompa el posicionamiento. Resultado: 8/8 campos y el botón clickeables, panel entero dentro del viewport. |
