# Optional: enables the Pydantic AI wording pass. Templates remain the fallback.
OPENAI_API_KEY=

# Optional: Slack incoming webhook URL. Without it, notify_slack.notify() is a no-op.
SLACK_WEBHOOK_URL=
