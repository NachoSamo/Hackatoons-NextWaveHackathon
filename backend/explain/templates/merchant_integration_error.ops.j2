The evidence indicates a merchant integration pattern across the affected providers. {{ evidence | join(' ') }} {{ action.rationale }} Re-evaluate in {{ action.reevaluate_after }}. This is simulation only.
