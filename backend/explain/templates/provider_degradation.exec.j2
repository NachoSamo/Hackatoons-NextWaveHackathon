{{ incident.diagnosis_category.replace('_', ' ').title() }} is affecting the selected payment slice. {% if cost %}Estimated revenue at risk is ${{ '{:,.0f}'.format(cost.usd_per_hour) }} per hour.{% endif %} Recommended next step: {{ action.title }}.
